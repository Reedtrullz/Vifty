{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vifty.local/schemas/viftyctl-run.schema.json",
  "title": "viftyctl run --json report",
  "description": "Machine-readable report emitted by viftyctl run --json after the child command has launched and exited. Pre-child failures still use the command-error schema.",
  "type": "object",
  "required": [
    "schemaVersion",
    "schemaID",
    "command",
    "coolingLeasePrepared",
    "autoRestoreAttempted",
    "autoRestoreSucceeded",
    "childExitCode",
    "autoRestoreError",
    "generatedAt"
  ],
  "properties": {
    "schemaVersion": {
      "const": 1
    },
    "schemaID": {
      "const": "https://vifty.local/schemas/viftyctl-run.schema.json"
    },
    "command": {
      "const": "run"
    },
    "coolingLeasePrepared": {
      "const": true
    },
    "autoRestoreAttempted": {
      "const": true
    },
    "autoRestoreSucceeded": {
      "type": "boolean"
    },
    "childExitCode": {
      "type": "integer"
    },
    "autoRestoreError": {
      "type": [
        "string",
        "null"
      ]
    },
    "resolvedChildExecutable": {
      "type": "string",
      "description": "Absolute executable path selected by viftyctl run child-command resolution before the cooling lease was requested. Present in current builds when runLifecycle.resolvedChildExecutableReported is true; absent in legacy run reports."
    },
    "resolvedChildExecutableSHA256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Lowercase SHA-256 digest of the resolved executable bytes, computed after child-command resolution and before the cooling lease is requested when the executable file is readable. Omitted when unavailable or in legacy run reports."
    },
    "generatedAt": {
      "type": "number"
    }
  },
  "additionalProperties": false
}
